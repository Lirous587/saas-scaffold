package {{.Domain}}

import (
	"github.com/gin-gonic/gin"
	"scaffold/internal/{{.Domain}}/handler"
)

func RegisterV1(r *gin.RouterGroup, handler *handler.HttpHandler) func() {
	// g := r.Group("/v1/{{.Domain}}")
	{
        // g.POST("/", handler.Create{{.DomainTitle}})
		// g.DELETE("/:id", handler.Delete{{.DomainTitle}})
		// g.PUT("/:id", handler.Update{{.DomainTitle}})
	}
	return nil
}
